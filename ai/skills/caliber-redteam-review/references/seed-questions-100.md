# 数据直通车 · 质询题库（100题）

1. 查询语句中使用了 `cust_type` 字段，但仅注释 '01个人 02企业'，未说明其他可能值（如'00未知'）如何处理。此字段在业务上是否只有这两个值？
2. SQL中 `where status = 'A'`，但未说明 status 字段的取值含义。'A'代表正常？还有哪些状态？
3. 查询条件包含 `create_time >= date_sub(current_date, interval 1 year)`，硬编码一年，但业务可能需要任意时间范围。是否应支持参数化？
4. 使用 `select *` 但实际只需要部分字段，存在敏感字段泄露风险（如证件号、手机号）。是否已标注敏感字段并配置脱敏规则？
5. 表 `cust_info` 与 `cust_addr` 通过 `cust_id` 关联，但未说明地址是否唯一（可能存在多个地址）。取数结果可能出现重复，如何处理？
6. 查询中使用了 `group by cust_id` 但未指定聚合函数，实际 SQL 可能不合法。文档中的 SQL 片段是否经过验证？
7. 字段 `phone` 在注释中说明“手机号”，但未区分是主手机号还是备用手机号。是否有多值？
8. 查询条件包含 `and name like '%李%'`，模糊查询可能导致全表扫描。是否应限制模糊查询必须配合其他索引条件？
9. 文档中 SQL 片段包含 `/*+ parallel(4) */` 提示，但未说明使用并行查询的前提条件。是否所有用户都能用？
10. 查询结果包含 `cust_id`，但不同表可能都有 `cust_id`，是否应明确来源表？
11. 文档中仅有一个 SQL 示例，但实际业务可能有多种查询维度（如按证件号、按手机号）。是否应拆分多个场景或 SQL 变体？
12. 字段 `update_time` 没有时区说明，可能存在时间偏差。是否需要统一为 UTC 或明确时区？
13. 查询中使用了 `rownum <= 100` 限制返回行数，但未说明是否按某种排序。可能导致每次结果不一致。
14. 字段 `email` 存在格式多样性，文档未说明如何处理非法邮箱。
15. 文档中未列出所有输入参数，仅给出固定条件。若作为场景，输入参数应明确定义。
16. 使用了 `cust_id in (select cust_id from blacklist)`，子查询可能效率低下。是否有优化方案？
17. 字段 `id_no` 证件号码未标注脱敏规则，直接展示可能违反隐私合规。
18. 文档中 SQL 包含注释 `-- 过滤掉测试客户`，但未说明测试客户的标准（如 cust_id 以 'test' 开头）。
19. 联表时使用了 `left join` 但未说明业务上是否允许地址缺失。应明确是否需要 inner join？
20. 查询条件 `and age between 18 and 60` 硬编码，但业务可能动态变化。是否应参数化？
21. 文档中 SQL 片段有多个版本（如历史表），但未标记适用时间范围。
22. 使用了 `nvl(phone,'无')` 处理空值，但 '无' 在业务上可能与其他含义混淆（如注销用户）。
23. 字段 `gender` 取值 'M'/'F'，但未说明是否还有其他值（如 'U'未知）。
24. 查询中 `order by update_time desc` 但未加索引，可能导致排序性能问题。是否应建议建索引？
25. 文档作者和验证人缺失，无法追溯信息来源。
26. `modify_type` 码值 '01','02','03' 在注释中有说明，但缺少 '04' 及后续扩展。如何管理新增码值？
27. 变更日志表 `retail_customer_modify_log` 中 `old_value` 和 `new_value` 字段内容随 `modify_type` 变化，但文档未说明每种类型下值的格式（如手机号变更时存完整手机号）。
28. 查询条件 `modify_time >= date_sub(current_date, interval 3 month)` 硬编码，业务可能需要更长时间范围。
29. 如果查询时间范围跨越多个月份，可能涉及分区表，但 SQL 未利用分区剪裁。
30. 注释说“避免全表扫描”，但未提供强制索引提示或最佳实践。
31. 联表 `customer_info` 时使用 `cust_id = cust_no`，但两个字段可能来自不同系统，存在一致性问题。
32. 文档中未列出变更日志表的主键和索引情况，可能影响查询性能。
33. 字段 `operator` 是操作人，但可能是员工号或姓名，未明确。
34. 变更日志可能包含敏感信息（如旧手机号），是否已标注敏感并脱敏？
35. 如果查询 `modify_type` 为 '01'（手机号变更），结果中 `old_value` 和 `new_value` 可能包含完整手机号，应脱敏。
36. 文档中的 SQL 仅查询了变更日志，未提供关联后的客户姓名，但业务常需姓名。是否应包含？
37. `modify_time` 存储精度是秒还是毫秒？与业务要求是否匹配？
38. 如果同一客户同一时间发生多个变更，日志中可能有多条记录，查询结果是否应去重或按需排序？
39. 注释提到“近 3 个月数据”，但未说明数据保留周期。超过 3 个月的数据是否被归档？如何查询历史？
40. 文档中 SQL 使用了 `select *`，但实际只需部分字段，存在数据冗余和合规风险。
41. 字段 `log_id` 是否为自增？在高并发下可能冲突？
42. 变更类型 '02' 地址变更，但地址可能拆分为多个字段（省、市、详细），日志只存一个字段是否合理？
43. 未说明变更日志是否包含操作渠道（如柜面、手机银行），如有需要，应增加渠道字段。
44. 查询中 `order by modify_time desc` 但未加索引，可能性能差。
45. 文档中 SQL 示例缺少异常处理，如日期参数无效时如何处理？
46. 变更日志表与主表 `customer_info` 可能存在数据不一致（如主表更新但日志未记录）。如何保证一致性？
47. 文档未说明 `operator` 字段是否关联员工表，如需操作人姓名，需二次联表。
48. 如果用户输入时间范围大于3个月，系统是自动拒绝还是允许？门禁策略如何？
49. 文档中无版本记录，无法知道此 SQL 对应哪个业务版本（如系统升级后日志结构变化）。
50. 同一 SQL 中同时查询变更日志和客户信息，但客户信息可能很大，是否应分批？
51. 代发明细涉及多张表（如批次表、明细表、客户表），但文档未给出表关系图，难以理解数据流。
52. SQL 中包含多个 `left join`，但未说明业务上空值如何处理（如批次表存在但明细表无数据是否仍展示批次）。
53. 查询条件 `batch_date between :startDate and :endDate` 使用参数，但未说明日期格式（yyyy-mm-dd 或 yyyymmdd）。
54. 代发金额字段 `amt` 未说明是否包含手续费，导致统计误差。
55. 使用了窗口函数 `row_number() over (partition by batch_id order by amt desc)` 取最大金额记录，但未说明业务意图。
56. 代发状态字段 `status` 取值 '00待处理','01成功','02失败'，但文档未列出完整映射。
57. 联表条件 `batch.corp_no = corp.corp_no`，但 corp_no 可能在不同表中含义不同（如企业号 vs 企业统一编号）。
58. SQL 中使用了 `union all` 合并多个子查询，但未说明数据来源表是否可能重复。
59. 查询中包含 `select *`，但明细表可能有大量字段，影响性能。
60. 文档中未说明查询结果是否需要分页，若数据量大可能导致内存溢出。
61. 代发批次表与明细表是 1:N 关系，查询结果可能因明细条数膨胀，需去重处理。
62. 使用了 `group by batch_id, status` 统计各批次状态数量，但未考虑状态可能为 NULL。
63. 字段 `trans_time` 存储了时分秒，但查询条件只用了 `batch_date`，可能导致跨天交易遗漏（如凌晨交易）。
64. 文档中 SQL 包含 `/*+ parallel(8) */` 提示，但未说明数据量阈值，可能在小表上浪费资源。
65. 代发失败原因字段 `fail_reason` 为自由文本，难以统计分析。是否应标准化为码值？
66. 联表 `employee` 表获取操作员姓名，但员工表可能变更频繁，是否需要快照？
67. 查询条件 `batch_date >= trunc(sysdate-30)` 硬编码 30 天，业务可能需要任意天数。
68. 代发明细表可能分区，但查询未利用分区键，可能导致全分区扫描。
69. 文档中未说明各表的数据量级和增长趋势，无法评估查询性能。
70. 使用了 `distinct` 去重，但未说明重复原因，可能掩盖数据质量问题。
71. 查询中包含 `order by batch_id, amt desc`，但未对 amt 建索引，可能导致排序性能问题。
72. 文档中 SQL 片段没有版本信息，当表结构变更时（如新增字段），旧 SQL 可能失效。
73. 查询条件 `and (remark is not null)` 可能排除部分业务数据（如无备注的明细），是否合理？
74. 代发涉及企业内部账户和外部账户，是否存在越权风险（查询他人账户）？
75. 文档中未提供任何示例输出，难以验证查询结果的正确性。
76. 分类树中“零售客户”与“对公客户”是否存在重叠（如个体工商户归哪类）？
77. 每个分类下是否有对应的场景清单？若没有，如何保证分类的实用性？
78. 分类层级过深（超过3级）可能导致用户难以找到所需场景。是否应扁平化？
79. 如果某场景依赖的字典项被废弃，但仍有已发布的场景引用它，发布门禁是否应阻止新版本发布？
80. 对于跨多个业务领域的数据查询（如零售客户同时涉及财富产品），如何设计场景的归属？一个场景可以属于多个主题节点吗？
81. 派生规则如果涉及多个表，当其中一个表的字段改名，如何自动发现并影响分析？
82. 离线评测集中是否应包含“模糊匹配”场景，例如用户输入近似但非精确的表名或字段名？
83. 当用户输入问句包含多个可能意图时，系统应如何生成多个PlanIR候选并让用户选择？这需要什么机制？
84. 对于复杂SQL中的窗口函数，是否有统一的命名规范或语义解释，以便业务人员理解？
85. 数据地图中的语义视图如何与物理表版本关联？当物理表重构（如拆分表）时，语义视图如何升级？
86. 在门禁判定中，如果`schema_link_score`较高但`reliability_score`较低（例如由于数据质量差），应如何决策？
87. 如何验证一个场景的SQL变体在数据量大的情况下执行效率？是否应引入执行计划预测？
88. 对于历史数据分表（如按年份分表），场景应如何设计取数方案？是否需要动态路由？
89. 当业务人员手动录入场景时，如何确保他们正确填写了`verified_at`？是否需要与测试结果关联？
90. 审计日志中的`validation_trace_id`能否在用户侧看到？是否需要提供界面让用户追溯查询结果的可信度？
91. 对于跨系统的数据（如核心系统与数据湖），如何建立统一的派生规则解决键不一致问题？
92. 如果两个场景使用了相同的源表但过滤条件不同，是否可能产生口径冲突？如何检测？
93. 如何衡量“不可答检测”的准确性？是否需要构建专门的拒答样本集？
94. 当LLM辅助导入时，如果抽取结果置信度低，系统应如何提示用户重点关注哪些字段？
95. 对于包含敏感字段的场景，发布时是否必须指定脱敏规则，并且脱敏规则本身是否需要版本管理？
96. 如果业务需求发生变化，需要修改已发布场景，变更流程是否必须重新验证并更新`verified_at`？
97. 如何确保主题节点的分类不随时间漂移？是否需要定期复审分类合理性？
98. 对于模糊查询（如`like '%keyword%'`），是否有性能阈值限制？是否应自动转换为全文检索？
99. 在离线回放评测中，如果某个场景的`boundary_violation_score`突然升高，如何自动触发告警并分配给责任人？
100. 知识图谱在M3阶段将用于增强规划，但在M1阶段我们是否应该开始收集实体关系数据（如表、字段、场景之间的关联）？如何收集？

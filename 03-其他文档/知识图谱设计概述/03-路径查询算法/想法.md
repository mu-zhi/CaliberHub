1.评分函数通过累乘实现打分
    评分函数=路径衰减指数* 每跳置信度* 每跳路径权重* (1+热度校正值)
2.所有与排名第一路径的数值差异在10倍以内的路径(或者什么别的阈值),认为是备选路径,并且有最大路径数限制
3.每个备选路径都会计算出向量值
4.如果存在备选路径,进行组内相对差异评价 (Relative Variance)
    步骤 A：构建相似度矩阵
    步骤 B：寻找“语义孤岛”（组内投票）
    步骤 C：决策触发（逻辑阈值）-直接输出第一名/触发背景补全要求

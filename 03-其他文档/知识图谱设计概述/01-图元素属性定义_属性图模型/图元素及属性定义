原子属性: 
    纯粹的键值对,可能包含约束条件
扩展属性:
    有时我们会有比较复杂的属性,这些属性包含了描述性的信息,它们来自暧昧模糊的语料库
    这些属性也有它自己必须的字段,以便于审计:
    attr_title:属性的标题,例如ele_uid就是一个属性标题,同一时间,同一个图元素下面只能有一个属性标题是激活的
    attr_uuid:由属性的标题+插入日期+属性来源字段组合而成
    is_active:属性的有效性
    attr_ins_tm:插入日期
    attr_exp_tm:失效日期
    attr_value:属性内容
    attr_src:属性来源(包含历次更新的知识来源) 
    同时,属性带有大量的可选字段,可以自由定义,以下是一些用来消除歧义的推荐属性字段命名表:
    attr_puuid:它的父母属性uuid
    attr_conf_score:置信度,如果该字段缺失默认为1.0,取决于新增的三元组的置信度:
        人为生成,人为确认=1.0: 元数据,sql批量录入等等
        人为输入,ai生成,人为确认=0.98
        ai生成+未人为确认=0.95
        ai推理+人为确认=0.9
        ai推理+未人为确认=0.8
        属性的conf_score反映了属性和它归属的图元素之间的关系可靠性;
        当它存在puuid时,应该计算puuid的置信度与新增知识来源置信度的平均值
    attr_desc:属性描述


图元素
    所有知识图谱中的点和边都是图元素
    要求强制包含以下原子属性:
    ele_uid:逻辑唯一标识符,同一时刻只能有一个is_active=True的ele_uid图元素
    ele_uuid:物理唯一标识符,由ele_uid+ele_ins_tm+ele_src拼接组合而成,永不更改
    ele_type:元素类型
    is_active:活动性标记
    ele_ins_tm:插入日期
    ele_exp_tm:失效日期    
    ele_src:创建这个图元素的语料来源
    ele_attr_msk:属性掩码,用于快速区分节点类型,确定这个节点里是否包含某些对寻路有效的属性    
    可选原子属性:    
    ele_puuid:创建这个图元素的父节点uuid
    ele_conf_score:描述图元素的整体置信度,缺省值为1,大部分时候不用它而是用单次查询时,依赖属性掩码激活的相关活动属性的置信度
    扩展属性
    除了原子属性外,图元素还可以包含任意多的扩展属性,不在这里定义
    
点元素dot:
    点元素是一种图元素,首先它拥有全部的图元素强制非空属性:
        ele_uid:逻辑唯一标识符,同一时刻只能有一个is_active=True的ele_uid图元素
        ele_uuid:物理唯一标识符,由ele_uid+ele_ins_tm+ele_src拼接组合而成,永不更改
        ele_type:元素类型
        is_active:活动性标记
        ele_ins_tm:插入日期
        ele_exp_tm:失效日期    
        ele_src:创建这个图元素的语料来源
        ele_attr_msk:属性掩码,用于快速区分节点类型,确定这个节点里是否包含某些对寻路有效的属性    
    其次它包含可选原子属性
        ele_puuid:创建这个图元素的父节点uuid
        ele_conf_score:描述图元素的整体置信度,缺省值为1,大部分时候不用它而是用单次查询时,依赖属性掩码激活的相关活动属性的置信度,点的置信度反映了它的"存在性",通常很少会有"可能不存在的点".但是比较有可能出现"这个点并不存在这个属性"的情况.
        值得注意的是,点或是置信度应该大于等于图
    然后它包含可选的扩展属性,比如:
        Popularity:热度,定期更新
  

边元素line:
    边元素也是一种图元素,首先它拥有全部的图元素强制非空属性:
        ele_uid:逻辑唯一标识符,同一时刻只能有一个is_active=True的ele_uid图元素
        ele_uuid:物理唯一标识符,由ele_uid+ele_ins_tm+ele_src拼接组合而成,永不更改
        ele_type:元素类型
        is_active:活动性标记
        ele_ins_tm:插入日期
        ele_exp_tm:失效日期    
        ele_src:创建这个图元素的语料来源
        ele_attr_msk:属性掩码,用于快速区分节点类型,确定这个节点里是否包含某些对寻路有效的属性    
    其次,它包含了边元素特有的一些强制非空属性
        line_from_uid:来源点的uid
        line_to_uid:通往点的uid
    接着,它会可能会拥有一些原子属性:
        ele_conf_score:边是强制拥有置信度的,但不一定必须显式存储,不存储置信度意味着这条边的置信度是1
    最后,边可能会包含一系列扩展属性比如:
        Popularity:热度,定期更新
        path_weight:路径权重,一些额外的奖励因素,比如:
            物理主外键 (FK)：1.2（极其可靠，优先走）。
            血缘映射 (Lineage)：1.0（标准路径）。            
            映射损耗系数：0.98(语义到物理层的损耗)
            向量点亮 (Vector)：0.8（语义相关但物理不一定通）。

规则元素
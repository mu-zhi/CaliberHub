# 08c-已发布快照检索索引同步与版本锁定测试报告

> 对应特性文档：`docs/architecture/features/iteration-02-runtime-and-governance/08c-已发布快照检索索引同步与版本锁定.md`
> 对应实施计划：`docs/plans/2026-04-22-published-snapshot-retrieval-index-sync-implementation-plan.md`
> 日期：2026-04-22
> 状态：passed

## 一、测试目标

验证已发布快照摘要已经被同步为实验检索索引清单，并满足：

1. 非 `PUBLISHED（已发布）` 快照不得进入实验索引。
2. 发布成功后必须为 `snapshot_id（快照标识）` 生成唯一 `index_version（索引版本）`。
3. 运行读取前必须按 `snapshot_id` 锁定索引；缺失时允许降级，不允许串快照。

## 二、执行记录

### 2.1 后端索引清单与锁定逻辑

- 执行命令：
  - `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) PATH="$JAVA_HOME/bin:$PATH" sh "/Users/rlc/Library/Application Support/JetBrains/IntelliJIdea2026.1/plugins/maven/lib/maven3/bin/mvn" -q -Dtest=ExperimentalRetrievalIndexSyncServiceTest,CanonicalSnapshotBindingServiceTest,KnowledgePackageApiIntegrationTest test`
- 结果：
  - `ExperimentalRetrievalIndexSyncServiceTest` 通过，锁定 `PUBLISHED` 限制、唯一 `indexVersion` 与回填锁定逻辑。
  - `CanonicalSnapshotBindingServiceTest` 通过，发布后会持久化实验索引清单。
  - `KnowledgePackageApiIntegrationTest` 通过，知识包读取链路能绑定已发布快照对应索引版本。

### 2.2 前端监控页最小可见反馈

- 执行命令：
  - `cd frontend && NODE_OPTIONS="--require=<rollup wasm hook>" npm test -- src/pages/MonitoringAuditPage.indexSync.test.jsx`
- 结果：
  - `MonitoringAuditPage.indexSync.test.jsx` 通过，监控页已展示 `实验索引版本`、`ACTIVE`、唯一 `index version` 与 `snapshot mismatch 0`。

## 三、覆盖结论

- 已覆盖：
  - 发布快照 -> 实验索引清单持久化。
  - `snapshot_id` -> `index_version` 唯一锁定。
  - 监控页的最小版本锁定可见反馈。
- 未覆盖：
  - 真正的退役与回滚动作编排界面。本轮先把台账、锁定与监控表达打通，正式回滚编排继续由 `11b` 和发布中心专题承接。

## 四、遗留风险

1. 当前实验索引清单 migration 实际落位为 `V22__experimental_retrieval_index_manifest.sql`，已与现有迁移序列对齐；如后续重写计划文档或重排迁移，需要保持编号口径同步。
2. 监控页展示仍为最小样板态；后续若接入真实运行告警流，需要把索引错配告警与发布中心回退动作进一步串联。

# 运行检索与澄清结果交互测试报告

> 对应特性文档：`docs/architecture/features/iteration-02-runtime-and-governance/10-数据地图浏览与覆盖追踪.md`（运行决策台部分）
> 测试文件：`frontend/src/pages/KnowledgePackageWorkbenchPage.test.jsx`、`frontend/src/pages/KnowledgePackageWorkbenchPage.interaction.test.jsx`
> 日期：2026-03-30
> 状态：passed

## 一、测试目标

验证运行决策台（KnowledgePackageWorkbenchPage）在以下维度的正确性：

1. **SSR 渲染**：各结果分支（allow / clarification / deny）的静态渲染正确性
2. **交互流程**：场景加载、表单提交、澄清子问题一键检索、返回修改、错误处理

## 二、SSR 渲染测试（KnowledgePackageWorkbenchPage.test.jsx）

| # | 场景 | 验证要点 | 结果 |
|---|------|---------|------|
| 1 | SSR 冒烟 — 加载态 | 渲染"正在加载已发布的代发明细场景与知识包样板资产" | PASS |
| 2 | SSR 冒烟 — 不崩溃 | renderToString 不抛异常 | PASS |
| 3 | 知识包 allow | 渲染"知识包摘要"、allow badge、字段裁剪（可见/脱敏/受限/禁止）、证据来源 | PASS |
| 4 | 知识包 clarification | 渲染"需要补充条件"、候选场景（含快照号）、子问题、澄清问题、追踪信息 | PASS |
| 5 | 澄清子问题按钮 | HTML 包含"用此子问题检索"和"返回修改查询"按钮 | PASS |
| 4b | 澄清互斥 | clarification 存在时不渲染"知识包摘要"和"字段裁剪" | PASS |
| 6 | 知识包 deny | 渲染 deny badge、受限/禁止字段，不渲染"需要补充条件" | PASS |
| 7 | IDENTIFIER_REQUIRED 错误 | SSR 初始态不崩溃 | PASS |
| 8 | 检索过程卡片 | 渲染场景召回原因、候选方案数、方案选择结果 | PASS |

## 三、交互流程测试（KnowledgePackageWorkbenchPage.interaction.test.jsx）

| # | 场景 | 验证要点 | 结果 |
|---|------|---------|------|
| 1 | 场景加载与表单渲染 | fetch 返回已发布场景后，渲染场景选择下拉和结构化查询表单 | PASS |
| 2 | 提交查询 — allow 结果 | 填写标识值、点击提交后渲染"知识包摘要"和"检索过程" | PASS |
| 3 | 提交查询 — clarification 结果 | 渲染"需要补充条件"、2 个「用此子问题检索」按钮、不渲染正常摘要 | PASS |
| 4 | 子问题按钮触发二次检索 | 点击「用此子问题检索」后发起新 API 调用，返回 allow 后切换到正常摘要 | PASS |
| 5 | 返回修改查询 | 点击「返回修改查询」后清空结果，回到"尚未生成知识包"空状态 | PASS |
| 6 | IDENTIFIER_REQUIRED 错误 | 后端返回 400 + code=IDENTIFIER_REQUIRED 时，前端展示友好提示 | PASS |

## 四、测试技术说明

- **SSR 测试**：Vitest 2.1.9 + Node 环境 + React renderToString
- **交互测试**：Vitest 2.1.9 + jsdom 环境 + @testing-library/react（本轮新引入）
- **Mock 策略**：`vi.stubGlobal("fetch", ...)` 按 URL 路径分发预设 JSON 响应
- **覆盖率**：15 个测试用例覆盖 3 种结果分支 + 2 种交互路径 + 1 种错误路径

## 五、本轮变更汇总

### 后端
1. **KnowledgePackageClarificationDTO**（新建）：强类型替代原 `JsonNode clarification`，含 `SceneCandidateDTO` 和 `PlanCandidateDTO` 嵌套 record
2. **KnowledgePackageDTO**：`clarification` 字段从 `JsonNode` 改为 `KnowledgePackageClarificationDTO`
3. **KnowledgePackageQueryAppService**：`clarification()` 方法重写为构造强类型 DTO，移除 Jackson ObjectNode/ArrayNode 手工拼接
4. 后端全量 87 测试通过（含 KnowledgePackageApiIntegrationTest 的 clarification 断言）

### 前端
1. **OpenAPI 类型**：`KnowledgePackageDTO` 补齐 `KnowledgePackageClarificationDTO` 嵌套类型
2. **澄清交互增强**：子问题「用此子问题检索」一键二次检索 + 「返回修改查询」按钮
3. **测试基础设施**：引入 `@testing-library/react`、`@testing-library/jest-dom`、`jsdom`
4. 前端全量 73 测试通过

## 六、遗留事项

无。原两项遗留均已在本轮解决：
- [x] 后端 `KnowledgePackageDTO` Java 类已同步强类型化 `clarification` 字段
- [x] 已引入 jsdom + React Testing Library 补齐交互流程测试

## 七、v2 覆盖增量

1. 澄清分支补齐 `planCandidates` 展示与断言
2. 澄清分支补齐 `mergeHints` 展示与断言
3. 后端 `KnowledgePackageApiIntegrationTest` 继续锁定 `MULTI_SCENE_AMBIGUOUS` 结果契约
